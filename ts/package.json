{
  "name": "dflockd-client",
  "version": "0.1.0",
  "description": "TypeScript client for dflockd distributed lock daemon",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
